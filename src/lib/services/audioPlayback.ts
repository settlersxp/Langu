// src/lib/services/audioPlayback.ts
import type { Section } from '../database/schema';

class AudioPlaybackService {
	async playForeignLanguageText(section: Section) {
		// Play foreign language audio
		// Wait 2x audio length for user repetition
	}

	async playEnglishTranslation(section: Section) {
		// Play English translation audio
	}
}
